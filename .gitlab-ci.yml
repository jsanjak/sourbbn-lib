image: "rikorose/gcc-cmake"

stages:
  - test

.test_template: &test_definition
  stage: test
  before_script:
    - cmake -H. -Bbuild
    - make -j8 -C build
  script:
    - ./build/bin/unit_tests

test_master:
  <<: *test_definition
  only:
    refs:
      - master
